#!/bin/bash

# Add config to timemachine
  echo "
[Homes]
    basedir regex = /home
    follow symlinks = true
    spotlight = true
    home name = $u's home(afp)" >> /etc/afp.conf

pkill -HUP afpd
exit 0
